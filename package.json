{
  "name": "chatboot",
  "version": "1.0.0",
  "description": "docker pull mongo:latest\r docker run -d --name mongodb -p 27017:27017 mongo\r docker ps\r docker exec -it mongodb mongosh\r db.messages.find()",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@google/generative-ai": "^0.2.1",
    "express": "^4.18.2",
    "mongodb": "^6.3.0"
  }
}
